syntax = "proto3";

package storage;

option go_package = "proto";

message FileRequest {
    oneof data {
        string file_name = 1;
        bytes chunks = 2;
    };
}

message FileResponse {
    uint32 size = 1;
}

service Storage {
    rpc Upload(stream FileRequest) returns (FileResponse);
}